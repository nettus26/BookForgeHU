<Window x:Class="BookForge.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BookForge"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- Cím -->
        <TextBlock Grid.Row="0"
                   Text="📚 BookForge"
                   FontSize="32"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>


        <!-- Gombok -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal">

            <Button Content="📂 EPUB hozzáadása"
                    Width="180"
                    Height="40"
                    Margin="0,0,10,0"/>

            <Button Content="🔄 Frissítés"
                    Width="120"
                    Height="40"/>
        </StackPanel>


        <!-- Könyvlista -->
        <GroupBox Grid.Row="2"
                  Header="Könyvtár"
                  Margin="0,20,0,0">

            <ListView>
                <ListViewItem>
                    📕 BookForge tesztkönyv
                    - BookForge
                </ListViewItem>

            </ListView>

        </GroupBox>


    </Grid>

</Window>
